/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CivilStatus } from '../../models/civil-status';

export interface UpdateCivilStatus$Params {

/**
 * ID del Civil Status a actualizar.
 */
  id: number;
      body: CivilStatus
}

export function updateCivilStatus(http: HttpClient, rootUrl: string, params: UpdateCivilStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<CivilStatus>> {
  const rb = new RequestBuilder(rootUrl, updateCivilStatus.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CivilStatus>;
    })
  );
}

updateCivilStatus.PATH = '/mdm/demographic/civil-status/{id}';
