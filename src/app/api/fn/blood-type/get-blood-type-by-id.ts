/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BloodType } from '../../models/blood-type';

export interface GetBloodTypeById$Params {

/**
 * ID del Blood Type a recuperar
 */
  id: number;
}

export function getBloodTypeById(http: HttpClient, rootUrl: string, params: GetBloodTypeById$Params, context?: HttpContext): Observable<StrictHttpResponse<BloodType>> {
  const rb = new RequestBuilder(rootUrl, getBloodTypeById.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BloodType>;
    })
  );
}

getBloodTypeById.PATH = '/mdm/blood-type/{id}';
