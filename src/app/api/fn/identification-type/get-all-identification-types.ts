/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IdentificationType } from '../../models/identification-type';

export interface GetAllIdentificationTypes$Params {
}

export function getAllIdentificationTypes(http: HttpClient, rootUrl: string, params?: GetAllIdentificationTypes$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<IdentificationType>>> {
  const rb = new RequestBuilder(rootUrl, getAllIdentificationTypes.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<IdentificationType>>;
    })
  );
}

getAllIdentificationTypes.PATH = '/mdm/identification-type/all';
