package com.amachi.app.vitalia.common.controller;

import com.amachi.app.vitalia.common.dto.PageResponseDto;
import org.springframework.http.ResponseEntity;


public interface GenericApi<D> {

//    /**
//     * Retrieves all records with pagination.
//     *
//     * @param pageIndex the index of the page (zero-indexed).
//     * @param pageSize the number of records per page.
//     * @return a ResponseEntity containing the paginated data wrapped in a PageResponse.
//     */
//    ResponseEntity<PageResponseDto<D>> getAll(Integer pageIndex, Integer pageSize);

}
